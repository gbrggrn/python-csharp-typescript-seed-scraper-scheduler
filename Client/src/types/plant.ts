export interface Plant {
    name: string,
    sowDepth: number,
    minGerminationDays: number,
    maxGerminationDays: number,
    minHeight: number,
    maxHeight: number,
    rowSpacing: number,
    plantSpacing: number,
    minSowMonth: number,
    maxSowMonth: number,
    minHarvestMonth: number,
    maxHarvestMonth: number
}