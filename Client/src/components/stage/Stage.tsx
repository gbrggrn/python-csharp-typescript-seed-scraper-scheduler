import { type Plant } from '../types/plant'

interface StageProps {
    selectedIds: string [];
    allPlants: Plant[];
}

const Stage = () => {
    return (
        <div>

        </div>
    )
}

export default Stage;